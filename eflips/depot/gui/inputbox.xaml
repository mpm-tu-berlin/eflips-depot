<!--
    author: B.Bober
    -->
<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:eflips"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Title="Depot GUI" Height="130" Width="500" Name="Window">
    <Grid Margin="4" FocusManager.FocusedElement="{Binding ElementName=TxtInput}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="148*"/>
            <ColumnDefinition Width="345*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <TextBlock Name="TxtDescription" Text="Description" Margin="4,4,3.333,3.667" Grid.ColumnSpan="2"/>
        <TextBox Grid.Row="1" Name="TxtInput" Margin="4,4.333,3.333,3.667" Grid.ColumnSpan="2" VerticalContentAlignment="Center" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.Column="1" Margin="0,0,0.333,-0.333">
            <Button Name="BtnOk" Content="OK" Width="100" Height="30" Margin="5" IsDefault="True" />
            <Button Content="Cancel" Width="100" Height="30" Margin="5" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>